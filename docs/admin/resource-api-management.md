# 资源与 API 管理规则

## 文件资源

- 上传入口统一走后端上传接口。
- 资源保存 URL、类型、大小、来源、创建时间。
- 图片和视频必须校验类型和大小。

## API 接口配置

- 用于管理可配置接口，不替代后端真实路由。
- 新增接口必须明确路径、方法、用途、状态。
- 不允许通过配置绕过鉴权。

## 资源接入中心

- 记录外部资源来源、同步状态、错误信息。
- 不向未知外部服务器发送用户数据。

## 验证

- 上传后后台可预览。
- C 端图片正常加载。
- 资源删除不影响仍在使用的内容。
