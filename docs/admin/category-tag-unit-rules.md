# 分类标签单位规则

## 分类管理

- 支持菜谱分类、食材分类、运营分类。
- 分类支持启用、禁用、排序。
- 禁用分类后，表单下拉不再显示。
- 已被内容引用的分类不能硬删除。

## 标签管理

- 标签用于 C 端展示和后台筛选。
- 标签支持分组、状态、排序。
- 不允许重复创建同名同类型标签。

## 单位管理

- 单位用于食材、价格、菜篮子、采购记录。
- 单位必须有类型，例如重量、容量、数量、包装。
- 涉及价格换算时必须明确基础单位。

## 验证

- 后台表单下拉是否同步。
- C 端筛选和标签展示是否正确。
- 禁用后是否不再出现在新增表单。
