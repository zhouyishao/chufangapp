# 图标规则

## 图标来源

- C 端：统一使用 `lucide-react`。
- 后台：优先使用 `@ant-design/icons`，也可使用 `lucide-react` 或 `react-icons`。

## 使用规则

- 同一页面不要混用多种图标风格。
- 图标必须服务于识别，不做装饰堆砌。
- 图标尺寸、线宽、颜色在同一模块内保持一致。
- 图标按钮必须有可点击区域和明确反馈。

## 禁止

- 禁止 emoji 作为主要功能图标。
- 禁止手写复杂 SVG 图标库。
- 禁止为单个页面引入新图标库。
